#!/usr/bin/env bash
# Installs and configures Nginx to listen on port 80 and return a page with Holberton School
apt-get update -y
apt-get install -y nginx
mkdir -p /var/www/html
echo "Holberton School for the win!" > /var/www/html/index.html
service nginx start
service nginx reload
